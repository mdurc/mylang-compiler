/*
Expected Output:
  42
*/

enum Result {
  Ok { val: i64 },
  Err { code: i64 }
}

func main() {
  mut r := Result::Ok{ val = 42 };
  switch (r) {
    case Result::Ok(data): { print data.val, "\n"; }
    case Result::Err(err): { print err.code, "\n"; }
  }
}
